{
  "slug": "command-palette-hook",
  "difficulty": "complex",
  "prompt": "Show a controlled dashboard command palette that uses useCommandPalette and opens with mod+k.",
  "reference": "import * as React from 'react';\nimport { CommandPalette, useCommandPalette, useCommandPaletteShortcut, type CommandPaletteCommand } from '@tale-ui/react/command-palette';\n\nconst commands: CommandPaletteCommand[] = [\n  { id: 'dashboard', title: 'Open dashboard', subtitle: 'View workspace metrics.', group: 'Navigation', href: '/dashboard', shortcut: ['Mod', '1'] },\n  { id: 'invite', title: 'Invite teammate', subtitle: 'Add a member to this workspace.', group: 'Actions', keywords: ['member', 'user'] },\n  { id: 'settings', title: 'Open settings', subtitle: 'Manage workspace preferences.', group: 'Settings', href: '/settings' },\n];\n\nexport function DashboardCommandPalette() {\n  const [open, setOpen] = React.useState(false);\n  const palette = useCommandPalette({\n    commands,\n    close: () => setOpen(false),\n    onAction(command) {\n      console.log('Run command:', command.id);\n    },\n  });\n\n  useCommandPaletteShortcut({ open, setOpen, shortcut: 'mod+k' });\n\n  return (\n    <CommandPalette.Root open={open} onOpenChange={setOpen} size=\"lg\">\n      <CommandPalette.Trigger>Search workspace</CommandPalette.Trigger>\n      <CommandPalette.Backdrop>\n        <CommandPalette.Popup aria-label=\"Search workspace\">\n        <CommandPalette.Close aria-label=\"Close command palette\" />\n        <CommandPalette.Content inputValue={palette.query} onInputChange={palette.setQuery}>\n          <CommandPalette.SearchField>\n            <CommandPalette.Input placeholder=\"Search dashboards and actions...\" />\n            <CommandPalette.ClearButton aria-label=\"Clear search\" />\n          </CommandPalette.SearchField>\n          <CommandPalette.ListBox aria-label=\"Workspace commands\">\n            {palette.groupedCommands.map((group) => (\n              <CommandPalette.Section key={group.id}>\n                <CommandPalette.SectionHeader>{group.title}</CommandPalette.SectionHeader>\n                {group.commands.map((command) => (\n                  <CommandPalette.Item key={command.id} command={command} onAction={() => void palette.runCommand(command)}>\n                    <CommandPalette.ItemContent>\n                      <CommandPalette.ItemTitle>{command.title}</CommandPalette.ItemTitle>\n                      <CommandPalette.ItemDescription>{command.subtitle}</CommandPalette.ItemDescription>\n                    </CommandPalette.ItemContent>\n                    {command.shortcut ? <CommandPalette.ItemMeta><CommandPalette.Shortcut keys={command.shortcut} /></CommandPalette.ItemMeta> : null}\n                  </CommandPalette.Item>\n                ))}\n              </CommandPalette.Section>\n            ))}\n          </CommandPalette.ListBox>\n          <CommandPalette.Footer>{palette.filteredCommands.length} results</CommandPalette.Footer>\n        </CommandPalette.Content>\n       </CommandPalette.Popup>\n      </CommandPalette.Backdrop>\n    </CommandPalette.Root>\n  );\n}",
  "tags": ["CommandPalette"]
}
